<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?android:attr/selectableItemBackground"
    android:clickable="true"
    android:orientation="horizontal"
    android:paddingBottom="@dimen/space_min"
    android:paddingEnd="@dimen/space_min"
    android:paddingStart="@dimen/space_small"
    android:paddingTop="@dimen/space_min"
    android:focusable="true">

    <TextView
        android:id="@+id/viewMessage1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/space_huge"
        android:background="@drawable/btn_chat_grey"
        android:padding="@dimen/space_small"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:textColor="@android:color/white"
        android:visibility="visible"
        android:text="@string/app_name"/>

</LinearLayout>
